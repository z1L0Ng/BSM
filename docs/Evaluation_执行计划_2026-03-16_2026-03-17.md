# Evaluation 执行计划（2026-03-16 晚 - 2026-03-17）

目标：按“今晚到明早 05:30 跑除 `gurobi+gurobi` 外组合，明天到酒店后补齐 `gurobi+gurobi`，最终产出 paper evaluation 图表与表格”执行。

## 0. 当前冻结口径

- 数据窗口：`06:00-22:00`
- 时间粒度：15 分钟
- 仿真时段数：64
- 配置文件：`configs/ev_yellow_2025_11_eval_6_22_fcfs.yaml`

## 1. 今晚（2026-03-16）到明早 05:30（America/Chicago）：跑除 `gurobi+gurobi` 外组合

### 1.1 gate（严格不含 Gurobi）

```bash
bash scripts/run_stress_gate_48_nogurobi.sh
```

- 默认只跑：
  - `heuristic_plus_fcfs`
- 产出：`results/baseline_combinations/stress_gate48_nogurobi_6_22.csv`

### 1.2 主矩阵（排除 `gurobi+gurobi`）

```bash
bash scripts/run_full_matrix_except_gurobi_gurobi_until_0530.sh
```

- 组合：`{gurobi, heuristic, ideal} × {gurobi, fcfs}` 去掉 `gurobi+gurobi`
- 总计：5 组合，`5×3×3×3=135` runs（按组合顺序串行）
- 产出：`results/baseline_combinations/full_matrix_except_gurobi_gurobi_6_22.csv`
- 截止：`2026-03-17 05:30` 前不启动新组合；未完成组合次日继续 `--resume`

### 1.3 今晚交付物

1. 非 Gurobi gate 汇总表（是否通过、异常点）
2. 非 Gurobi 主矩阵关键 KPI 表：
   - `total_served`
   - `battery_swap_success_ratio`
   - `deadline_miss_ratio`
   - `charging_deadline_miss_ratio`
   - `max_station_total_power_kw`
   - `total_waiting_vehicles`

## 2. 明天（2026-03-17）执行：补齐 `gurobi+gurobi`

### 2.1 跑 `gurobi+gurobi` 组合

```bash
python scripts/run_param_sweep.py \
  --config configs/ev_yellow_2025_11_eval_6_22_fcfs.yaml \
  --inventories 5,10,20 \
  --chargers 3,5,8 \
  --swap-capacities 3,6,9 \
  --reposition-solver gurobi \
  --charging-solver gurobi \
  --output-csv results/baseline_combinations/_tmp_full_matrix_except_gurobi_gurobi_6_22/algorithm_plus_gurobi.csv \
  --resume
```

- 产出后与前夜结果合并为 `full_matrix_6combo_6_22.csv`

### 2.2 稳定性诊断补证

```bash
python scripts/run_reposition_diagnostic.py \
  --config configs/ev_yellow_2025_11_eval_6_22_fcfs.yaml \
  --output-csv results/diagnostics/reposition_trace_full_6_22.csv
```

- 用于给出 `Status/SolCount/Runtime/outcome` 的可审计证据。

## 3. 结果整理（直接用于 paper evaluation）

### 3.1 表格

1. 主表（6 组合平均表现）
2. 瓶颈场景表（hard slice）
3. 稳定性附表（fallback/retry/no-incumbent 统计）

### 3.2 图表

1. 组合 KPI 对比图（bar）
2. `served / miss / waiting / peak power` 随容量变化曲线（line）
3. 2 组 episode 时序图对比：
   - `charging_power_kw`
   - `station_total_power_kw`
   - `waiting_vehicles`

## 4. 风险控制

1. 长任务中断后，`run_param_sweep.py --resume` 断点续跑。
2. 明天上电后先跑 1 组 smoke，再启动全量 162-run。
3. 历史旧口径结果（24h）与新口径结果（06:00-22:00）必须分开汇报，避免混用。

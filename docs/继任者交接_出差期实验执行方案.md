# 继任者交接：出差期实验执行方案

更新时间：2026-03-16  
当前基线提交：`7f3f046`

## 1. 当前状态（已执行）

- 已停止正在运行的 gate 任务：
  - `bash scripts/run_stress_gate_48.sh ...`
  - `python scripts/run_param_sweep.py ... --reposition-solver gurobi --charging-solver fcfs ...`
- 当前工作区未提交内容：
  - 修改：`docs/TODO_仿真有效性改进清单.md`
  - 新文件：`scripts/run_stress_gate_48.sh`

## 2. 用户最新决策（必须遵守）

1. 先不跑 Gurobi 相关全量任务。  
2. 先跑“不带 Gurobi”的所有脚本/组合。  
3. 24h episode 改为仅考虑 `06:00-22:00` 的数据窗口，以降低计算量。  
4. Gurobi 全量任务推迟到用户到酒店后、可稳定通电通宵时再启动。  

## 3. 继任者第一步清单

### A. 先确认“06:00-22:00”口径

- 现有时间粒度为 15 分钟，则时段数为 64（16 小时）。
- 需要统一三处口径：
  - 需求切片（只取 06:00-22:00）
  - 仿真 `horizon`
  - 对比实验中的所有 baseline/ablation 使用同一窗口

### B. 先执行非 Gurobi 任务

- 优先运行不含 Gurobi 的组合（例如 `heuristic+fcfs`, `ideal+fcfs`）。
- 输出要求：
  - 参数矩阵结果 CSV
  - 汇总表（served / swap_success / miss / waiting / peak）
  - 与历史结果口径差异说明（尤其是 06:00-22:00 窗口变化）

### C. Gurobi 延后执行

- 等用户确认“可通宵+稳定供电”后，再启动 Gurobi 组合。
- 启动前先保证：
  - 每个 case 完成即落盘（避免中断丢整组）
  - 日志实时 flush（可见进度）
  - 支持断点续跑

## 4. 风险提醒

- 当前 `run_param_sweep.py` 是“整组结束才写文件”，中断会丢进度。
- 本地机器若睡眠/断电，任务会停止；断网对本地 Academic license 通常影响较小，但休眠/断电是硬风险。


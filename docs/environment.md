# Environment

This project uses a single conda environment defined in `environment.yml`.

## Create and Activate

```bash
conda env create -f environment.yml
conda activate bsm-sim
```

## Update

```bash
conda env update -f environment.yml --prune
```
